# ==================== 服务配置 ====================
# 服务监听端口
BROWSER_SERVICE_PORT=2025
# 服务监听地址（0.0.0.0 允许外部访问）
BROWSER_SERVICE_HOST=0.0.0.0

# ==================== 浏览器配置 ====================
# 无头模式（true=后台运行，false=显示浏览器窗口）
HEADLESS=true
# 浏览器实例池大小（建议：2-5，每个约 200-400MB 内存）
BROWSER_POOL_SIZE=3
# 每个浏览器实例的最大并发页面数（理论最大并发 = POOL_SIZE × MAX_CONCURRENT_PAGES）
MAX_CONCURRENT_PAGES=10
# 空闲超时时间（秒），浏览器空闲超过此时间后自动重启释放内存（设为 0 禁用）
BROWSER_IDLE_TIMEOUT=5

# ==================== 日志配置 ====================
# 日志级别：DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
